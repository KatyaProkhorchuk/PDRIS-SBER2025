apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  sessionAffinity: None
  type: LoadBalancer
  selector:
    app: custom-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000